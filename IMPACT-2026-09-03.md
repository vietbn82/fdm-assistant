# Báo cáo ảnh hưởng — bộ preset trước / sau khi Viet dựng lại (03/09)

So sánh **giá trị hiệu dụng** (đã giải chuỗi kế thừa), không phải override thô.

- **Trước** = bộ `Novi … @AC KX` + `Anycubic Kobra X 0.4 nozzle - high quality`,
  đọc từ `git show HEAD~1:presets/…` (commit `aefe5ac`, 02/09).
- **Sau** = kho preset đang chạy, đọc bằng `--show` sau `--export` (commit `3d702b3`).
- Số tốc độ "thật" lấy từ `--audit --flow` (trần flow filament 13 mm³/s).

---

## 1. Tóm tắt theo kịch bản in

| Kịch bản | Ảnh hưởng gộp | Nặng nhất |
|---|---|---|
| Figure 1 màu (0.12 / 0.16) | 🟡 vừa | mặt trên in 150 thay vì 80; lớp đầu 50 thay vì 30; đùn ít hơn 4% |
| Figure 0.20 | 🟢 nhẹ | gần như không đổi, chỉ mặt trên + tường ngoài nhanh hơn; cấu trúc kế thừa sạch hơn |
| Đồ dùng (TOOL 0.20) | 🔴 nặng | tường 4 → 2, infill 25% → 15%: mất phần lớn thiết kế độ bền |
| Bản thử (TEST 0.28) | 🟢 nhẹ | thêm brim tự động, bỏ vài khoá seam/wipe |
| Bất kỳ, **nhiều màu** | 🟡 vừa | `purge_in_prime_tower = 0` trở lại + TOOL hết thu hồi purge |
| Bất kỳ, **một màu** | 🟡 vừa | machine preset Single Color rút 0.8 thay vì 1.6 |

🔵 Ba thay đổi có mặt ở **mọi** bản in, không phụ thuộc preset process:
`filament_flow_ratio` 1.0 → 0.96, `z_hop` 0.2 → 0.4 + `Slope Lift`,
`overhang_fan_threshold` 25% → 50%.

---

## 2. Tầng filament — áp cho mọi bản in

| Key | Trước | Sau | Hiệu ứng vật lý | Mức |
|---|---|---|---|---|
| `filament_flow_ratio` | **1.0** *(pin, P14/P15)* | 0.96 *(hãng)* | đùn ít hơn 4%. Đường 0.42 mm ra thật ~0.403; vỏ 2 tường 0.87 → 0.835 mm | 🔴 |
| `nozzle_temperature_HS` (BBL) | 205 | 203 | −2 °C, gần như không cảm nhận được | ⚪ |
| `nozzle_temperature_HS` (Generic) | 205 | **200** | −5 °C: bám lớp kém hơn, bớt tơ. Chính mức P28 đã thử rồi bỏ 30/08 | 🟡 |
| `overhang_fan_threshold` | 25% | 50% | quạt chỉ vọt lên khi phần đua ≥ 50% bề rộng. Phần đua 25–49% giờ nguội chậm hơn | 🟡 |
| `slow_down_min_speed` | 10 | 20 | sàn tốc độ khi lớp quá ngắn tăng gấp đôi → lớp nhỏ **ít được làm chậm** để nguội. Ảnh hưởng đúng chỗ figure chi tiết nhỏ | 🟡 |
| `slow_down_layer_time_HS` | 5 | *(không còn trong chuỗi)* | ngưỡng thời gian tối thiểu mỗi lớp về mặc định slicer | 🔵 |
| `filament_density` (BBL) | 1.3 | 1.24 | chỉ sai số ước tính khối lượng/giá (~5%), không đổi bản in | ⚪ |
| `filament_retraction_length` | `nil` | `nil` | 🟢 giữ đúng — filament không đè retraction của machine | 🟢 |
| `textured_plate_temp` / `_initial_layer` | 50 / 55 | 50 / 55 | 🟢 không đổi | 🟢 |
| `pressure_advance` | 0.036 | 0.036 | 🟢 không đổi, vẫn chờ đo lại | 🟢 |

🔴 **Dòng đầu là thay đổi ảnh hưởng rộng nhất trong cả đợt.** Pin 1.0 sinh ra từ
P14 (29/08) để chữa thiếu nhựa/tường mỏng; bỏ pin là quay lại đúng trạng thái
trước bản sửa đó.

---

## 3. Tầng machine

Trước: một preset dùng cho gần như mọi bản in (`- high quality`), bản `- TEST`
chỉ khác `z_hop`. Sau: tách theo **số màu**.

| Key | Trước *(mọi bản in)* | Sau — MultiColor | Sau — **Single Color** | Mức |
|---|---|---|---|---|
| `retraction_length` | 1.6 | 1.6 | **0.8** | 🟡 |
| `retraction_speed` | 45 | 45 | **30** | 🟡 |
| `deretraction_speed` | 35 | 35 | **30** | 🔵 |
| `retraction_minimum_travel` | 0.5 | 0.5 | **1** | 🔵 |
| `retract_before_wipe` | 100% | 100% | **0%** | 🟡 |
| `z_hop` | 0.2 | **0.4** | **0.4** | 🟢 |
| `z_hop_types` | Normal Lift | **Slope Lift** | **Slope Lift** | 🟡 |
| `purge_in_prime_tower` | 1 | **0** | **0** | 🟡 |
| `machine_end_gcode` | gốc hãng *(P31)* | gốc hãng | gốc hãng | 🟢 |

**Đọc bảng này thế nào:**

- 🟡 **In một màu giờ chạy retraction gốc hãng.** Rút 0.8 mm thay vì 1.6, chậm
  hơn (30 vs 45), và lau **trước** khi rút xong thay vì sau. Buồng nóng chảy
  79 mm³ dùng chung là lý do bộ cũ tăng lên 1.6 — lý do đó không mất đi khi in
  một màu. Triệu chứng nếu sai: tơ, cục nhựa ở điểm bắt đầu.
- 🟢 `z_hop` 0.4 lớn hơn `max_layer_height` 0.28 → **hết** rủi ro nozzle va vào
  lớp vừa in ở process 0.28, và hết luôn việc phải nhớ đổi Printer preset.
- 🟡 `Slope Lift` nâng chéo — đầu phun lướt qua mặt in trong lúc nâng. P10 đổi
  sang `Normal Lift` chính vì thế. Đổi lại có thể để vết trên mặt trên.
- 🟡 `purge_in_prime_tower = 0`: bằng chứng gcode 30/08 cho thấy khi in nhiều màu
  **không có support**, không còn đường xả hợp lệ nên purge bị bỏ qua → màu mới
  lẫn màu cũ ở những đường đầu tiên.

---

## 4. Tầng process — từng preset

### 4.1 `0.12 Figure` *(cũ: `Novi 0.12 - FIGURE`)*

| Key | Trước | Sau | Hiệu ứng | Mức |
|---|---|---|---|---|
| `top_surface_speed` | 80 | **150** | mặt trên nhanh gấp 1.9×. Flow chỉ 7.6 mm³/s nên **không** bị trần hạ — 150 là tốc độ thật. P21 sinh ra vì mặt trên xấu ở đúng mức này | 🔴 |
| `outer_wall_speed` | 50 | **60** | +20%, cạnh có thể lộ rung nhẹ | 🟡 |
| `initial_layer_speed` | 30 | **50** | lớp đầu nhanh hơn 67% → ít thời gian ép dính bàn | 🟡 |
| `sparse_infill_pattern` | gyroid | **3dhoneycomb** | infill hết đẳng hướng; với figure hầu như không quan trọng | 🔵 |
| `detect_thin_wall` | 1 | **0** | chi tiết mảnh hơn một đường đùn có thể bị bỏ qua | 🟡 |
| `small_perimeter_threshold` | 20 | **0** | `small_perimeter_speed = 50%` không bao giờ kích hoạt → lỗ/trụ nhỏ in ở tốc độ đầy đủ | 🟡 |
| `sparse_infill_density`, `prime_tower_width`, scarf, `wipe_*`, `reduce_crossing_wall` | | **không đổi** | | 🟢 |

### 4.2 `0.16 FIGURE`

Giống 0.12 ở bốn dòng đầu, thêm:

| Key | Trước | Sau | Hiệu ứng | Mức |
|---|---|---|---|---|
| `initial_layer_infill_speed` | 50 | **100** | infill lớp đầu nhanh gấp đôi | 🟡 |
| `top_shell_layers` | 6 *(hãng)* | 6 *(hãng)* | 🟢 không đổi — `profiles/process.md` cũ ghi 5 là **sai**, preset chưa bao giờ đè khoá này | 🟢 |
| `outer_wall_speed` | 50 | 60 | như trên | 🟡 |
| `top_surface_speed` | 80 | 150 | như trên; flow 10.1 mm³/s < 13 nên là tốc độ thật | 🔴 |

### 4.3 `0.20 Figure` — 🟢 ít ảnh hưởng nhất

| Key | Trước | Sau | Hiệu ứng | Mức |
|---|---|---|---|---|
| Preset cha | `0.16mm HQ` + `layer_height = 0.2` vá tay | **`0.20mm HQ`** | cấu trúc đúng; `support_*_z_distance` giờ khớp layer 0.2 mà không cần pin | 🟢 |
| `outer_wall_speed` | 50 | 60 | +20% | 🟡 |
| `top_surface_speed` | 80 | 150 | mặt trên nhanh 1.9× | 🔴 |
| `initial_layer_speed` / `_infill_speed` | 30 / 50 | **30 / 50** | 🟢 giữ nguyên — bản duy nhất còn bộ chống bong bàn | 🟢 |
| shell, infill, prime tower, scarf | | **không đổi** | | 🟢 |

### 4.4 `0.20 TOOL` — 🔴 ảnh hưởng nặng nhất

| Key | Trước | Sau | Hiệu ứng | Mức |
|---|---|---|---|---|
| `wall_loops` | **4** | **2** | vỏ mỗi bên 1.77 mm → 0.87 mm: **−51% tiết diện tường**. Độ bền uốn/xoắn của vật in FDM đến chủ yếu từ tường | 🔴 |
| `sparse_infill_density` | 25% | **15%** | −40% nhựa infill | 🔴 |
| `wall_sequence` | inner-outer-inner | mặc định *(inner/outer)* | liên kết giữa các tường kém hơn | 🟡 |
| `bottom_shell_layers` | 4 | **3** | đáy 0.8 → 0.6 mm | 🔵 |
| `outer_wall_speed` | 120 | 200 → **thật 154** | +28% tốc độ thật, dung sai cạnh xấu đi | 🟡 |
| `flush_into_objects` / `_infill` | 1 / 1 | **0 / 0** | nhựa purge không còn được thu hồi vào thân/infill | 🟡 |
| `skirt_loops` | 2 | **0** | không còn mồi nhựa trước khi vào vật | 🟡 |
| `small_perimeter_threshold`, seam/wipe, `sparse_infill_pattern = gyroid` | | **không đổi** | | 🟢 |

🔴 Cộng lại: preset TOOL giờ chỉ còn khác `0.20mm Standard` của hãng ở
infill pattern + vài khoá seam/wipe. Vật in bằng nó **nhẹ hơn, tốn ít nhựa hơn,
in nhanh hơn — và yếu hơn đáng kể**. Ước lượng thô: tiết diện chịu lực giảm ~50%
ở vùng tường, tổng vật liệu giảm ~25–30%.

### 4.5 `0.28 TEST` — 🟢 nhẹ

| Key | Trước | Sau | Hiệu ứng | Mức |
|---|---|---|---|---|
| `brim_type` | `no_brim` | **`auto_brim`** | tự thêm brim: bám bàn tốt hơn, phải cắt gọt sau in | 🔵 |
| `skirt_loops` | 2 | **0** | mất bước mồi | 🟡 |
| `seam_gap` | 15% | **10%** | hở ít hơn ở điểm khép vòng → dễ có cục nhựa nhỏ | 🔵 |
| `small_perimeter_threshold` | 20 | **0** | như trên | 🔵 |
| `wipe_on_loops` / `wipe_before_external_loop` | 1 / 1 | **0 / 0** | không lau trước khi vào tường ngoài | 🔵 |
| `slowdown_for_curled_perimeters` | 1 | **0** | không chậm lại ở mép cong vênh | 🔵 |
| infill 10% 3dhoneycomb, shell 2/2, `flush_into_*` | | **không đổi** | | 🟢 |
| Phải đổi Printer preset khi in? | **có** *(bản `- TEST`)* | **không** | bớt một bước dễ quên | 🟢 |

---

## 5. Định lượng những thứ đo được

| Chỉ số | Trước | Sau | Đổi |
|---|---|---|---|
| Hệ số đùn (mọi bản in) | 1.00 | 0.96 | **−4%** |
| Tường TOOL, tiết diện mỗi bên | 1.77 mm | 0.87 mm | **−51%** |
| Infill TOOL | 25% | 15% | **−40%** |
| Mặt trên FIGURE, tốc độ thật | 80 mm/s | 150 mm/s | **+88%** |
| Tường ngoài FIGURE | 50 mm/s | 60 mm/s | +20% |
| Tường ngoài TOOL, tốc độ thật *(sau trần flow)* | 120 mm/s | 154 mm/s | +28% |
| Rút nhựa khi in 1 màu | 1.6 mm @ 45 mm/s | 0.8 mm @ 30 mm/s | **−50% chiều dài** |
| Nâng Z mỗi lần travel | 0.2 mm, thẳng | 0.4 mm, chéo | +0.2 mm |
| Purge 12 lần đổi màu *(ma trận 29/08)* | 3854 mm³, TOOL thu hồi phần lớn | 3854 mm³, **TOOL thành rác** | ≈ +4.8 g rác mỗi bản TOOL 4 màu |
| Tổng số khoá override *(9 preset, bỏ metadata)* | **136** | **71** | **−48%** |
| — machine | 10 + 10 | 5 + **0** | Single Color rỗng hoàn toàn |
| — process | 20 / 20 / 23 / 15 / 13 | 11 / 11 / 13 / 7 / 6 | TOOL mỏng nhất: 15 → 7 |
| — filament | 15 + 10 | 10 + 8 | |

🔵 Trần flow che bớt một phần thay đổi tốc độ: `inner_wall_speed = 300` của TOOL
thực tế chỉ chạy 144 mm/s cả trước lẫn sau, nên phần "nhanh hơn" không lớn như
con số trong UI.

---

## 6. Được gì

| | |
|---|---|
| 🟢 Cấu trúc | `0.20 Figure` hết kế thừa sai cha, hết vá `layer_height` — B1 đóng sau 9 ngày |
| 🟢 An toàn cơ khí | `z_hop` 0.4 ≥ `max_layer_height` 0.28: hết rủi ro nozzle va lớp vừa in, hết bước đổi Printer preset khi in 0.28 |
| 🟢 Bảo trì | preset mỏng hơn hẳn → tự theo kịp khi hãng cập nhật profile gốc (đúng nguyên tắc `docs/preset-model.md` mục 2) |
| 🟢 Sạch | `filament\base\` rỗng, không còn file trùng tên; `--audit` 0 lỗi |
| 🟢 Rõ ý đồ | tách machine preset theo **số màu** dễ hiểu hơn tách theo `z_hop` |

## 7. Mất gì — xếp theo mức rủi ro

| # | Mất | Bằng chứng lịch sử | Triệu chứng sẽ thấy |
|---|---|---|---|
| 1 | `filament_flow_ratio = 1.0` | P14/P15 (29/08) sinh ra để chữa thiếu nhựa | tường mỏng, mặt trên hở khe, lớp bám kém |
| 2 | TOOL: `wall_loops = 4`, infill 25%, `wall_sequence` | thiết kế độ bền của preset TOOL | đồ dùng nứt/gãy ở tải từng chịu được |
| 3 | `top_surface_speed = 80` | P21 (29/08) sinh ra vì mặt trên xấu ở 150 | mặt trên gợn, đường không đều |
| 4 | `purge_in_prime_tower = 1` | P30 (30/08), đọc trực tiếp từ gcode | lem màu vài đường đầu sau mỗi lần đổi màu, khi bản in không có support |
| 5 | Retraction 1.6/45 khi in một màu | P9/P26, buồng 79 mm³ | tơ, cục nhựa ở điểm bắt đầu |
| 6 | `z_hop_types = Normal Lift` | P10 | vết lướt trên mặt trên |
| 7 | `initial_layer_speed = 30` (0.12/0.16) | bộ sửa bám bàn | bong góc lớp đầu |
| 8 | TOOL: `flush_into_objects/_infill` | bật 23/08 | +4.8 g rác mỗi bản 4 màu |

## 8. Hoàn tác từng phần — giá trị cần pin lại

❌ Chưa áp gì. Đây là danh sách tra cứu; muốn áp thì chốt trong `TODO.md`
(B2/B3/B4) rồi mới sang `PENDING_APPLY.md`.

| Nếu thấy | Pin lại | Ở preset |
|---|---|---|
| tường mỏng / thiếu nhựa | `filament_flow_ratio = 1` | cả hai filament |
| đồ dùng yếu | `wall_loops = 4`, `sparse_infill_density = 25%`, `wall_sequence = inner-outer-inner wall` | `0.20 TOOL @AC KX` |
| mặt trên xấu | `top_surface_speed = 80` | ba bản FIGURE |
| lem màu sau đổi màu | `purge_in_prime_tower = 1` | `Kobra X 0.4 - MultiColor` |
| tơ khi in một màu | `retraction_length = 1.6`, `retraction_speed = 45`, `retract_before_wipe = 100%` | `Kobra X 0.4 - Single Color` |
| bong góc lớp đầu | `initial_layer_speed = 30` | `0.12 Figure`, `0.16 FIGURE` |
| vết lướt mặt trên | `z_hop_types = Normal Lift` | cả hai machine preset |

🟡 Mỗi khoá pin lại là một khoá ngừng theo kịp cập nhật của hãng. Chỉ pin khi đã
**thấy** triệu chứng, đừng pin phòng xa.
